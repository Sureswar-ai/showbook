import { describe, it, expect } from "vitest";
/**
 * Placeholder spec — the real seat-lock test requires a live Redis instance
 * and is kept for manual verification. See README's "5-minute demo script"
 * step 3 which exercises the concurrency guarantee end-to-end.
 */
describe("SeatLockService (placeholder)", () => {
  it("is defined in the module", () => {
    expect(true).toBe(true);
  });
});
